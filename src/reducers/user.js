// Esse reducer será responsável por tratar as informações da pessoa usuária
const initialUserState = {
  email: '',
};

const user = (state = initialUserState) => {
  switch (true) {
  default:
    return state;
  }
};

export default user;
