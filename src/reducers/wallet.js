// Esse reducer será responsável por tratar o todas as informações relacionadas as despesas
const initialWalletState = {
  currencies: [],
  expenses: [],
};

const wallet = (state = initialWalletState) => {
  switch (true) {
  default:
    return state;
  }
};

export default wallet;
